from my_unit import MyUnit
from unit_attacks import Attacks
from unit_attacks import moveSet

class Fish():
    # (FILENAME, X, SPEED, SPAWN_COOLDOWN, MAX_HEALTH, RANGE, ATTACK, ATTACK_COOLDOWN, UNIT_TYPE, LEVEL=1)
    def __init__(self, WANTEDFISH):
        self.__FISH = "Nothing"
        if WANTEDFISH == "Salmon":


